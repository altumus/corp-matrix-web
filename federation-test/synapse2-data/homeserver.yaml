# Configuration file for Synapse.
#
# This is a YAML file: see [1] for a quick introduction. Note in particular
# that *indentation is important*: all the elements of a list or dictionary
# should have the same indentation.
#
# [1] https://docs.ansible.com/ansible/latest/reference_appendices/YAMLSyntax.html
#
# For more information on how to configure Synapse, including a complete accounting of
# each option, go to docs/usage/configuration/config_documentation.md or
# https://element-hq.github.io/synapse/latest/usage/configuration/config_documentation.html
server_name: "server2.localhost"
pid_file: /data/homeserver.pid
listeners:
  - port: 8008
    resources:
    - compress: false
      names:
      - client
      - federation
    tls: false
    type: http
    x_forwarded: true
database:
  name: sqlite3
  args:
    database: /data/homeserver.db
log_config: "/data/server2.localhost.log.config"
media_store_path: /data/media_store
registration_shared_secret: "hTK=z_xjPOV-E4O_4eMImF^fx4,gvoXeZ,=7R#6n28A,_aX0fR"
report_stats: false
macaroon_secret_key: "X0RCHi.^#*4h#=lbjzU=UF3#eKz-iMQ9jATlT-BXKhiD9epopt"
form_secret: "GNQvl5._ytFiQimBjP6kEik7#OE&Td-H9EGaT;i~~ch;WzTrjB"
signing_key_path: "/data/server2.localhost.signing.key"
trusted_key_servers:
  - server_name: "matrix.org"


# vim:ft=yaml
enable_registration: true enable_registration_without_verification: true registration_shared_secret: "test-shared-secret-2"
federation_domain_whitelist:
  - server1.localhost

federation_verify_certificates: false 
